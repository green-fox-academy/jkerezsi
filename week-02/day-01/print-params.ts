// -  Create a function called `printParams`
//    which logs to the console the input parameters
//    (can have multiple number of arguments)

'use strict';

let array = [ 23 , 24 , 34 , 345325];

function printParams(array) {
    return array;
}

console.log(printParams(array));